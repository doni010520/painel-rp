// Versão do app — INCREMENTAR a cada mudança/deploy. A imagem Docker é taggeada
// com este mesmo valor (ver .github/workflows/deploy.yml), então a tag que você
// sobe no EasyPanel é exatamente a que aparece no topo da tela e em /api/version.
export const APP_VERSION = "v1.0.4";
